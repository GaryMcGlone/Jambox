import { DateTimeConvertPipe } from './date-time-convert.pipe';

describe('DateTimeConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
