import { StringConvertPipe } from './string-convert.pipe';

describe('StringConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new StringConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
